import GroupIcon from '../assets/GroupIcon.svg';

const Group = () => {
    return (
        <img
            src={GroupIcon}
            alt="Group design"
            className="relative w-full max-w-full overflow-hidden"
            style={{ height: '1651.5px' }}
        />
    );
};

export default Group;
